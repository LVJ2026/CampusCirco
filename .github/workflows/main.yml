name: Synchroniser Grist

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Vérification du secret
        run: |
          if [ -z "${{ secrets.GRIST_API_KEY }}" ]; then
            echo "Secret introuvable"
            exit 1
          else
            echo "Secret trouvé"
          fi
